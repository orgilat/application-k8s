apiVersion: v1
kind: Service

metadata:
  name: web-service
  namespace: application

spec:
  type: ClusterIP

  selector:
    app: web

  ports:
    - port: 5173
      targetPort: 5173