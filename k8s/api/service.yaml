apiVersion: v1
kind: Service

metadata:
  name: api-service
  namespace: application

spec:
  type: ClusterIP

  selector:
    app: api

  ports:
    - port: 3000
      targetPort: 3000