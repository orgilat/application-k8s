apiVersion: v1
kind: Service

metadata:
  name: {{ include "exposure.fullname" . }}-api

spec:
  selector:
    app: api

  ports:
    - port: 3000
      targetPort: 3000

  type: ClusterIP