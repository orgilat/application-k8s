global:
  resolve_timeout: 5m

route:
  receiver: local-dashboard
  group_by:
    - alertname
    - service
    - severity
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: local-dashboard
